#include <bits/stdc++.h>
using namespace std;

map<char,vector<pair<char,int>>> m;
char start;

int dfs(char start,int total){
	//操作
	for(auto p:m[start]){
		//traverse子節點
	}
}

int main(){
	char a,b;
	int k;
	//輸入
		cin>>a>>b>>k;
		m[a].push_back({b,k});
	return 0;
}